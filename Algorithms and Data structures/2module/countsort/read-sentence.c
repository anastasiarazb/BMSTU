#include <stdio.h>
#include <stdlib.h>
#include <string.h>

//void scan_sentence()
//{
//    while((letter = getchar()) != '\n'){
////        if(letter == ' ') continue;
//        ungetc(letter, stdin);
//        scanf(" %s", source+i);
//        i += strlen(source+i)+1;
//    }
//}
